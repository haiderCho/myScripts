## ✏️BulkFileRenamer.py
This script renames all files in a specified folder sequentially (e.g., `file_1`, `file_2`, …) while keeping their original extensions.

### Usage  
```bash
python BulkFileRenamer.py
```

### Configuration  
Edit the `folder` path inside the script to your target directory.

### Requirements  
* Python 3.8+


## 🔄excel_to_csv.py  
This script converts an Excel file (`.xlsx`) to a CSV file (`.csv`) using pandas.

### Usage  
```bash
python excel_to_csv.py
```

### Requirements  
* Python 3.8+
* pandas (`pip install pandas`)
* openpyxl (`pip install openpyxl`)